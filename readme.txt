para ejecutar la api

primero tienes que tener xamp instalado
después de esto debes tener node instalado
ahora las dependencias que instalaremos para correr la api todas estas dependencias tienen que estar instaladas en dentro de la carpeta por ejemplo …/…/trabjo: --> cd node
				…/…/trabjo/node: npm i express ejemplo

instalar express
npm install express
instalar Morgan
npm install morgan
instalar elpaquete de MySQL
npm install mysql
instalar nodemon
npm install nodemon
dotenv
npm install dotenv

opcional el wildware de cors en caso que quiera manejar en diferentes dominios 
npm install cors

-------------------------------------------------------------------frontend---------------------------------------
pip install flet

cd first-flet-app --- este nombre se pude cambiar
python -m venv .venv
.venv\Scripts\activate